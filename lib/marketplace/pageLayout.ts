/** Shared storefront content width — matches header (`MarketplaceShell`). */
export const MARKETPLACE_CONTENT_MAX = "max-w-7xl";

export const MARKETPLACE_PAGE_FONT =
  "font-[family-name:var(--font-plus-jakarta-sans)] text-[#2A4C6A]";

/** Cancels shell padding so page sections can use full content column width. */
export const MARKETPLACE_PAGE_OUTER = "-mx-4 -my-8 min-h-full px-4 py-8";

export const MARKETPLACE_PAGE_INNER = `mx-auto w-full ${MARKETPLACE_CONTENT_MAX}`;

export const MARKETPLACE_MAIN_CLASS = `mx-auto w-full ${MARKETPLACE_CONTENT_MAX} px-4 py-6 sm:px-6 sm:py-8`;
