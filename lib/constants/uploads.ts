/** URL path prefix for files under `public/uploads/`. */
export const UPLOAD_URL_PREFIX = "/uploads";

/** Default root folder segment (server uses `getUploadRootFolder()` from env). */
export const UPLOAD_CLIENT_SLUG = "glowish";
